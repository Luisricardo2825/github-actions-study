name: Pipeline

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Imprimir tag
        run: | 
              TAG=$(echo "$GITHUB_REF" | sed 's/refs\/tags\///')
              echo "Tag: ${{ github.event.release.tag_name }}"
